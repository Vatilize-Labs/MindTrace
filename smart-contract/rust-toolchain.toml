# Pinned for reproducible contract builds. Safe to bump: since the
# soroban-sdk 22 upgrade there is no known incompatibility with newer rustc.
[toolchain]
channel = "1.95.0"
targets = ["wasm32-unknown-unknown"]
