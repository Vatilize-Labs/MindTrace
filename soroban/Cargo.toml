[package]
name = "mindtrace-soroban"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "20"

[dev-dependencies]
soroban-sdk = { version = "20", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "z"
lto = true
